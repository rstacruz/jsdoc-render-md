/**
 * Does stuff.
 *
 * @returns {x}
 */

function lol () {
}

/**
 * A function with params.
 * @param {string=} name - The name of the person to greet
 */

function greet (name) {
  return 'hi ' + name
}

/**
 * lol
 * @type {boolean}
 */

var LOL = true

/**
 * sup
 * @class
 */
class X {}
